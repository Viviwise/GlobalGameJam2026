using UnityEngine;
[CreateAssetMenu(fileName = "SoundEvent", menuName = "Test/Events/SoundEvent")]
public class SoundEvent : EventForScene
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
